#include <stdio.h>

int main(void)
{
    while(1)
    {
        switch(menu())
        {
            case :
                break;
            case :
                break;
            case :
                break;
            case :
                break;
            case :
                break;
        }
    }

    return 0;
}