/* 
    练习 11-3：重构字符串复制演示程序

【背景】
你已实现了一个字符串复制函数 str_copy，其功能是将源字符串
复制到目标内存区域，并返回指向复制后字符串首字符的指针。

【任务】
编写一个完整的演示程序，要求如下：

1. 实现 str_copy 函数
   - 参数：目标指针（可写）、源指针（只读）
   - 功能：逐字符复制，包括结束符'\0'
   - 返回：指向目标字符串首字符的指针

2. 实现 main 函数
   - 定义两个字符数组：一个用于存储初始/结果字符串，一个用于暂存用户输入
   - 显示初始字符串内容
   - 提示用户输入新字符串
   - 读取用户输入
   - 执行复制操作
   - 显示复制完成提示
   - 显示复制后的字符串内容

3. 改进要求（与原题一致）
   - 将 str_copy 函数的调用直接作为 printf 函数的实参
   - 目的是利用 str_copy 的返回值，实现链式调用
   - 思考：这样可以省略哪一行代码？

【提示】
- 目标数组必须可写（不能用指针指向字符串字面量）
- str_copy 返回的是目标字符串的首地址
- printf 的 %s 格式符需要 char * 类型参数

【预期效果】
程序运行后，用户输入新字符串，程序显示复制后的结果，
且代码比原版本更简洁（减少了一行独立调用 str_copy 的代码）。
*/

/* d 指向目标字符串首元素的指针, s 指向源字符串首元素的指针*/

#include <stdio.h>

enum Parameter{ Length = 128 };

char *str_copy(char *d, const char *s);

int main(void)
{
    char str[Length] = "1234";//可写
    // char *ptr = "1234";
    char tmp[Length] = {0};
    
    printf("原字符串是:%s \n", &str[0]);
    // printf("原字符串是:%s \n", ptr);

    printf("要复制的字符串是:");
    scanf("%s", tmp);

    // ptr = tmp;

    printf("复制后的字符串是:\"%s\"\n", str_copy(&str[0], &tmp[0]));
    // printf("复制后的字符串是:\"%s\"\n", ptr);

    return 0;
}

char *str_copy(char *d, const char *s)
{
    int i = 0;
    //将源字符串的 首元素后第i个元素 按目标字符串中 首元素后的第i个字符位置 覆写
    while(d[i] = s[i]/* , d[i] */)
    {
        i++;
    }

    return d;/* 形参d未曾改变 */
}