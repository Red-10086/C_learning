enum Const_numbers { name_Len = 64 , student_Number = 3};
enum Name_index { first, second, third };

typedef struct student 
{
    char name[name_Len];
    int height;
    float weight;
    long schols;
} junior;